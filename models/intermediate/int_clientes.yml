version: 2

models:
  - name: int_clientes
    description: "Tabela intermediária que une dados de clientes e nomes de pessoas."
    columns:
      - name: id_cliente
        description: "Chave primária do cliente."
        tests:
          - unique
          - not_null

      - name: nome_cliente
        description: "Nome completo do cliente."
