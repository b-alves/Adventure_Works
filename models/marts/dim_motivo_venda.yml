version: 2

models:
  - name: dim_motivo_venda
    description: "Dimensão contendo os motivos de venda associados aos pedidos."
    columns:
      - name: id_pedido
        description: "Chave estrangeira para a tabela fato de vendas."
        tests:
          - not_null
          - relationships:
              to: ref('fact_vendas')
              field: id_pedido

      - name: motivo_venda
        description: "Motivo pelo qual o cliente realizou a compra."
